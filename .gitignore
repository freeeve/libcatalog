# Go
/bin/
/lcat
*.test
*.out
coverage.*
# Local-dev workspace (tasks/185): backend <-> root working-tree wiring
# stays out of the published modules.
/go.work
/go.work.sum

# Hugo (module consumers)
/public/
/resources/_gen/
.hugo_build.lock

# Node (Hugo module assets, if any)
node_modules/

# Terraform / Lambda deploy artifacts
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
bootstrap
lcatd*.zip

# Editor / OS
.DS_Store
*.swp
