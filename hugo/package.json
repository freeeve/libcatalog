{
  "name": "libcat-hugo-a11y",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only accessibility + JS tests for the libcat Hugo module. Not shipped: Hugo consumes only the templates and assets, never this package.",
  "scripts": {
    "test:a11y": "node a11y_audit.js exampleSite/public",
    "test:js": "node availability_test.cjs && node negatives_test.cjs && node sidebar_test.cjs",
    "test:links": "node link_check.cjs exampleSite/public",
    "search:index": "npx -y pagefind --site exampleSite/public"
  },
  "devDependencies": {
    "axe-core": "^4",
    "jsdom": "^24"
  }
}
