# 052 -- Paradise polish (later; split as needed)

## Context

Post-MVP quality-of-life items proposed in the Tier 2 plan, all optional and
independent; split into separate tasks when picked up.

## Scope

- Per-cataloger work queues / assignments (assign works/concerns/import batches;
  "My queue"; claim-next keystroke).
- ISBN quick-add: scan/paste ISBN -> external search across targets -> best
  match into a draft (thin sugar over copy cataloging).
- Session summaries / editing stats off the audit log.
- Optional AI-assist `Enricher` hook: queue-suggestions only, off by default,
  deployment supplies the provider (per the embeddings guardrail -- never
  default, never direct-write).
- Profile admin UI (runtime editing of editing profiles; MVP keeps them
  JSON-in-repo).

## Acceptance

Per item when picked up; nothing here blocks 1.0.
